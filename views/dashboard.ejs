<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              background: "rgb(218, 227, 229)",
              primaryText: "rgb(33, 33, 33)",
              accent: "rgb(105, 170, 215)",
              cta: "rgb(35, 150, 127)",
              highlight: "rgb(244, 96, 54)",
            },
          },
        },
      };
    </script>
</head>
<body class="bg background text-primaryText">
    <header class="bg-cta text-primaryText p-4 font-bold">
        <%- include("./partials/navbar.ejs") %>
      </header>
    <h1 class="font-bold text-center my-5 text-4xl">Dashboard</h1>
    <div class="p-4 my-3">
        <% if (users && users.length> 0) { %>
        <h2 class="font-bold m-2 text-2xl">Users</h2>
        <ul class="grid grid-cols-4 gap-4">
          <% users.forEach(user => { %>
          <li class="p-6 bg-background rounded-lg shadow-md border border-accent ">
            <strong class="font-semibold mb-2">Username:</strong>
            <%= user.username %> <br />
            <strong class="mb-1">Email:</strong>
            <%= user.email %> <br />
            <strong class="mb-2">Phone No.:</strong>
            <%= user.phone %> <br />
            <a href="/users/<%= user.id %>">
            <button class="mt-3 w-1/3 bg-red-500 text-white font-bold py-2 px-4 rounded hover:bg-highlight">
                View
            </button></a>
          </li>
          <% }) %>
        </ul>
        <% } else { %>
        <p>No users created yet.</p>
        <% } %>
      </div>
      <a href="/register" class="no-underline">
        <button class="block mx-auto bg-accent text-primaryText p-3 rounded">
          Register New User
        </button>
      </a>
</body>
</html>